    client:
      loadBalancer:
        servers:
          - url: "http://${CLIENT_HOST}:${CLIENT_PORT}"
        # Forward the original Host header to the backend service
        passHostHeader: true
